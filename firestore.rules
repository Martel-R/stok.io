
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra temporária para desenvolvimento: permite leitura e escrita para todos.
    // ATENÇÃO: Essas regras não são seguras para produção.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
